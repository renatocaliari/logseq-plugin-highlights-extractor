{
  "name": "logseq-plugin-highlights-extractor",
  "version": "0.0.1",
  "description": "Extract highglights from blocks",
  "main": "dist/index.html",
  "targets": {
    "main": false
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build --no-source-maps index.html --public-url ./"
  },
  "keywords": [
    "highlights"
  ],
  "author": "cali (renato caliari)",
  "license": "MIT",
  "dependencies": {
    "@logseq/libs": "0.0.6"
  },
  "logseq": {
    "id": "logseq-plugin-highlights-extractor",
    "title": "Highlights Extractor",
    "icon": "./icon.png"
  },
  "devDependencies": {
    "parcel": "^2.0.0-beta.2"
  }
}
